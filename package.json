{
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build --compile --minify --sourcemap src/index.ts --outfile backend"
  },
  "dependencies": {
    "hono": "^4.2.3",
    "meilisearch": "^0.45.0",
    "mongoose": "^8.8.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
